<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.I230622"
        tools:targetApi="33">

        <!-- ✅ MainActivity as the Launcher (Splash + Auth Check) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication / Signup / Flow Activities -->
        <activity android:name=".SecondActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".FourthActivity" />

        <!-- Main Feed and Posting -->
        <activity android:name=".FifthActivity" />
        <activity android:name=".UploadPostActivity" />

        <!-- Additional Screens (App Continuation) -->
        <activity android:name=".SixthActivity" />
        <activity android:name=".SeventhActivity" />
        <activity android:name=".EighthActivity" />
        <activity android:name=".NinthActivity" />
        <activity android:name=".TenthActivity" />
        <activity android:name=".EleventhActivity" />
        <activity android:name=".TwelfthActivity" />
        <activity android:name=".ThirteenthActivity" />
        <activity android:name=".FourteenthActivity" />
        <activity android:name=".FifteenthActivity" />
        <activity android:name=".SixteenthActivity" />
        <activity android:name=".SeventeenthActivity" />
        <activity android:name=".EighteenthActivity" />
        <activity android:name=".NinteenthActivity" />
        <activity android:name=".TwentiethActivity" />
        <activity android:name=".TwentyFirstActivity" />

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>